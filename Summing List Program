fun sum_to_n n =
    let
        fun helper (0, acc) = acc
            | helper (k, acc) = helper (k - 1, acc + k)
    in
        helper (n, 0)
    end;

sum_to_n 5;
(* 15 *)

sum_to_n 10;
(* 55 *)

sum_to_n 0;
(* 0 *)
