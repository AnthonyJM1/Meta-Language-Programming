fun two_functions (C, D, n, m) =
    case (C, D) of
         ([], []) => []
        | (x::xs, y::ys) => (x*n + y*m) :: two_functions(xs, ys, n, m);


two_functions([1,2,3], [6,7,8], 5, 2);
(* [17, 24, 31] *)

two_functions([4,5], [9,10], 2, 3);
(* [35,40] *)

two_functions([], [], 10, 5);
(* [] *)
